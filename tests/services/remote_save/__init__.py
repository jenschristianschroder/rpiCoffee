"""Tests for the remote-save FastAPI service.

Run from the repo root:
    pytest tests/services/remote_save/ -v

These tests use FastAPI's TestClient so no running container is needed.
Dataverse API calls are mocked — no real credentials required.
"""
